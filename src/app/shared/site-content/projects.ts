import { environment } from 'src/environments/environment';
import { IProjects } from '../global.model';

const projects: Array<IProjects> = [{
  title: '',
  content: [''],
  link: environment.angular_certificate,
  tools: ['']
}];

export default projects;
